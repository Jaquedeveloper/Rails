<%= form_for(@tipotelefone) do |f| %>
  <% if @tipotelefone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipotelefone.errors.count, "error") %> prohibited this tipotelefone from being saved:</h2>

      <ul>
      <% @tipotelefone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
