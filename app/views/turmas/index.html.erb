<h1>Listing turmas</h1>

<table>
  <tr>
    <th>Aluno</th>
    <th>Professordisciplina</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @turmas.each do |turma| %>
  <tr>
    <td><%= turma.aluno %></td>
    <td><%= turma.professordisciplina %></td>
    <td><%= link_to 'Show', turma %></td>
    <td><%= link_to 'Edit', edit_turma_path(turma) %></td>
    <td><%= link_to 'Excluir', turma, method: :delete, data: { confirm: 'Deseja excluir?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Turma', new_turma_path %>
